%tr.history
  %td.type
    = t"types.history.#{history.history_type.name}"
  %td
    .description
      = history.description.html_safe
    .info
      = history.date
      = t(".author")
      = history.user.email