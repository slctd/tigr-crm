%h1= t(".deals")
.buttons-panel
  = link_to t(".new_deal"), new_deal_path, class: "add"
  = link_to t("import.title"), deals_import_step_1_path, class: "import"
  = link_to t('.export'), deals_path(format: 'csv'), class: "export"

= render 'filter_form'

%table.table
  = render @deals